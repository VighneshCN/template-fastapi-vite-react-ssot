# Frontend Architecture (Vite + React)

## Toolchain
- Node + npm
- Vite build
- Vitest + React Testing Library
- ESLint

## Conventions (placeholders)
- Components in src/components
- Pages in src/pages (if you use routing)
- API client in src/api
